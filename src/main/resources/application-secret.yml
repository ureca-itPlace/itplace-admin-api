spring:
  config:
    activate:
      on-profile: secret

  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: "jdbc:mysql://localhost:3306/itplace?useSSL=false&serverTimezone=Asia/Seoul&allowPublicKeyRetrieval=true"
    username: root
    password: dkwnwhdk1!

  data:
    mongodb:
      uri: "mongodb://localhost:27017/itPlace"